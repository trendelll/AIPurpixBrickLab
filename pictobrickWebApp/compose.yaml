# Comments are provided throughout this file to help you get started.
# If you need more help, visit the Docker Compose reference guide at
# https://docs.docker.com/go/compose-spec-reference/

# Here the instructions define your application as a service called "server".
# This service is built from the Dockerfile in the current directory.
# You can add other services your application may depend on here, such as a
# database or a cache. For examples, see the Awesome Compose repository:
# https://github.com/docker/awesome-compose
services:

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
      args:
        # NEXT_PUBLIC_* is inlined into the client JS bundle at build time.
        # Safe to commit — Clerk publishable keys are public by design.
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_bWVhc3VyZWQtY29sbGllLTc3LmNsZXJrLmFjY291bnRzLmRldiQ
    ports:
      - "3000:3000"
    env_file:
      - .env.local    # secrets (CLERK_SECRET_KEY) — gitignored, share via password manager
    environment:
      NODE_ENV: production
      PORT: 3000
      FASTAPI_INTERNAL_URL: http://fastapi:8000
      NEXT_PUBLIC_API_URL: http://localhost:3000
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/pictobrick
      REDIS_URL: redis://redis:6379
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_bWVhc3VyZWQtY29sbGllLTc3LmNsZXJrLmFjY291bnRzLmRldiQ
    depends_on:
      - postgres
      - redis
      - fastapi

  postgres:
    image: postgres:15-alpine
    ports:
      - "5432:5432"       # exposed for local dev/debugging
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pictobrick
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"       # exposed for local dev/debugging

  fastapi:
    build:
      context: ./ml
      dockerfile: Dockerfile.fastapi
    ports:
      - "8000:8000"
    environment:
      REDIS_URL: redis://redis:6379
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/pictobrick
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data:

# The commented out section below is an example of how to define a PostgreSQL
# database that your application can use. `depends_on` tells Docker Compose to
# start the database before your application. The `db-data` volume persists the
# database data between container restarts. The `db-password` secret is used
# to set the database password. You must create `db/password.txt` and add
# a password of your choosing to it before running `docker-compose up`.
#     depends_on:
#       db:
#         condition: service_healthy
#   db:
#     image: postgres
#     restart: always
#     user: postgres
#     secrets:
#       - db-password
#     volumes:
#       - db-data:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=example
#       - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
#     expose:
#       - 5432
#     healthcheck:
#       test: [ "CMD", "pg_isready" ]
#       interval: 10s
#       timeout: 5s
#       retries: 5
# volumes:
#   db-data:
# secrets:
#   db-password:
#     file: db/password.txt

